<div class="container">
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">Eat-Da-Burger</h1>
            <p class="lead">Order your own burger, or devour our recommended burgers.</p>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-6">
            <div class="title">
                <h2>Ready to be devoured!</h2>
            </div>
            <div class="ready-burgers">
                <ul>
                    {{#each burgers}}
                        {{#unless devoured}}
                            {{> burgers/burger-block devoured=false}}
                        {{/unless}}
                    {{/each}}
                </ul>

            </div>
        </div>


        <div class="col-6">
            <div class="title">
                <h2>Devoured! YUMMMM!</h2>
            </div>
            <div class="devoured-burgers">
                <ul>
                    {{#each burgers}}
                    {{#if devoured}}
                    {{> burgers/devoured-block devoured=true}}
                    {{/if}}
                    {{/each}}
                </ul>

            </div>
        </div>

    </div>


    <div class="row">
        <div class="title" align="center">
            <h2>Build your own burger!</h2>
        </div>
        <div align="center">
            <form class="create-burger">
                <div class="form-group">
                    <label for="burger-name">New Burger: </label>
                    <input type="text" id="burger-name" ></input>
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>

    </div>
</div>
